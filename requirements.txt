# NOTE: torch is intentionally NOT listed here. The Render build command
# installs the CPU-only wheel (torch==2.13.0+cpu) from the PyTorch index
# to keep the build small; the CUDA bundle on PyPI is ~2.5 GB.
# For local dev, install torch separately (see README).
Flask==3.1.3
nltk==3.10.3
numpy==2.5.2
pandas==3.0.5
scikit-learn==1.9.0
reportlab==5.0.0
gunicorn==23.0.0
