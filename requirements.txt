# torch comes from the PyTorch CPU index: the plain "torch" on PyPI bundles
# CUDA (~2.5 GB); the +cpu wheel is ~200 MB and all we need. Pinning the
# +cpu local version forces pip to pick it from the extra index.
--extra-index-url https://download.pytorch.org/whl/cpu
Flask==3.1.3
torch==2.13.0+cpu
nltk==3.10.3
numpy==2.5.2
pandas==3.0.5
scikit-learn==1.9.0
reportlab==5.0.0
gunicorn==23.0.0
