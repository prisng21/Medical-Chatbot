services:
  - type: web
    name: meddy
    runtime: python
    plan: free
    region: oregon
    buildCommand: "pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu && pip install -r requirements.txt"
    startCommand: "gunicorn app:app --timeout 120"
    healthCheckPath: /
